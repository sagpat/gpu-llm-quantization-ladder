apiVersion: v1
kind: Pod
metadata:
  name: mistral-fp16-oom
spec:
  restartPolicy: Never
  tolerations:
  - key: sku
    operator: Equal
    value: gpu
    effect: NoSchedule
  containers:
  - name: loader
    image: pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime
    resources:
      limits:
        nvidia.com/gpu: 1
    env:
    - name: HF_TOKEN
      value: "<HF-TOKEN>"
    command: ["/bin/bash", "-c", "pip install -q 'transformers==4.40.0' 'accelerate==0.29.0' && python3 /phase1.py"]
    volumeMounts:
    - name: script
      mountPath: /phase1.py
      subPath: phase1.py
  volumes:
  - name: script
    configMap:
      name: phase1-script
